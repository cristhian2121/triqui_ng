import { SelectedBoxDirective } from './selected-box.directive';

describe('SelectedBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedBoxDirective();
    expect(directive).toBeTruthy();
  });
});
